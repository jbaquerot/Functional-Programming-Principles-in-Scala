package streams
import Bloxorz._
object test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(60); val res$0 = 
	BloxorzSuite;System.out.println("""res0: <error> = """ + $show(res$0))}
}
